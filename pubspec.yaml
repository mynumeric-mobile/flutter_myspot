name: flutter_myspot
description: Build easily tutorial in your flutter applications. Animate spotlight following a desired scenario, play audio messages an comments to guide users.
version: 1.0.2
homepage: https://github.com/mynumeric-mobile/flutter_myspot

# This package supports all platforms listed below.
platforms:
  android:
  ios:
  web:

environment:
  sdk: '>=3.0.6 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.0
  audioplayers: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter: